import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">
        該頁面不存在。
      </Alert>
    </div>
  );
};

export default PageNotFound;
